language: python
sudo: required
addons:
  apt:
    packages:
      - pandoc
      - texlive-xetex
      - texlive-latex-base
      - texlive-latex-extra
      - texlive-latex-recommended
      - texlive-fonts-recommended
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
install:
    - git clone https://github.com/libgit2/libgit2.git; mkdir libgit2/build; cd libgit2/build; cmake ..; sudo cmake --build . --target install; cd ../../
    - pip install pypandoc
    - pip install -r requirements.txt
    - python setup.py install
    - python -c "import pymmd; pymmd.build_mmd()"
script:
    - py.test test/tests.py
